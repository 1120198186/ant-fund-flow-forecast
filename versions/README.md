# Major versions

Each major modeling direction lives in exactly one `vNNN_short_name/` folder. A version folder owns its source, configs, notebooks, models, metrics, predictions, and submissions.

Create a folder with `python3 scripts/new_version.py v002_example --title "Example direction"`, then add its lineage and status to `REGISTRY.md`.

